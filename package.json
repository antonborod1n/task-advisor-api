{
  "name": "task-advisor-api",
  "version": "1.0.0",
  "description": "Mock REST API for Task Advisor (json-server)",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.6"
  },
  "engines": {
    "node": ">=18"
  }
}
